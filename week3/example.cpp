#include <iostream> 

using namespace std;

void showArray(double array[], int size) {
    for (int i = 0; i < size; i++) {
        cout << array[i] << endl;
    }

    cout << endl;
}

void fillArray(double array[], int size, double value) {
    for (int i = 0, i < size; i++) {
        arr[i] = value;
    }
}

double addElements(double array[], int size) {
    double sum = 0;
    for(int i = 0; i < size; i++) {
        total += array[i];
    }

    return sum;
}

int main() {
    double hour;
    double hours[5];    // First time you see a number in the []
                        // That number is a size

    
    return 0;
}